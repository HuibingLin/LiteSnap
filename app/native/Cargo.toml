[package]
name = "native"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2.16", features = ["napi4"] }
napi-derive = "2.16"
screenshots = "0.8"
image = "0.24"

[build-dependencies]
napi-build = "2.1"
